export default function() {


// const alphabetAutoHeight = document.getElementById('alphabetAutoHeight');

// if (!alphabetAutoHeight) return;

// const wordNL = document.querySelectorAll('.alphabet__word');
// const wordList = Array.prototype.slice.call(wordNL);

// const wordListOne = wordList.slice(0,wordList.length / 6)
// const wordListTwo = wordList.slice(wordList.length / 6, wordList.length / 3)
// const wordListThree = wordList.slice(wordList.length / 3, wordList.length / 2)
// const wordListFour = wordList.slice(wordList.length / 2, wordList.length / 1.5)
// const wordListFive = wordList.slice(wordList.length / 1.5, wordList.length / 1.2)
// const wordListSix = wordList.slice(wordList.length / 1.2, wordList.length / 1)

// console.log(wordListOne)
// console.log(wordListTwo)
// console.log(wordListThree)
// console.log(wordListFour)
// console.log(wordListFive)
// console.log(wordListSix)

// if(window.innerWidth < 768) {
// 	alphabetAutoHeight.style.height = alphabetAutoHeight.offsetHeight/2 + 'px';
// } else if(window.innerWidth < 1200) {
// 	alphabetAutoHeight.style.height = alphabetAutoHeight.offsetHeight/3 + 'px';
// } else {
// 	alphabetAutoHeight.style.height = alphabetAutoHeight.offsetHeight/6 + 'px';
// }

// if(window.innerWidth < 768) {
// 	var col1 = wordListOne.concat(wordListTwo,wordListThree);
// 	var col2 = wordListFour.concat(wordListFive,wordListSix);

// 	var col1Height = 0;
// 	col1.forEach(word => {col1Height += word.offsetHeight});

// 	var col2Height = 0;
// 	col2.forEach(word => {col2Height += word.offsetHeight});

// 	alphabetAutoHeight.style.height = Math.max(col1Height,col2Height) + 'px'

// } else if(window.innerWidth < 1200) {
// 	var col1 = wordListOne.concat(wordListTwo);
// 	var col2 = wordListThree.concat(wordListFour);
// 	var col3 = wordListFive.concat(wordListSix);

// 	var col1Height = 0;
// 	col1.forEach(word => {col1Height += word.offsetHeight});

// 	var col2Height = 0;
// 	col2.forEach(word => {col2Height += word.offsetHeight});

// 	var col3Height = 0;
// 	col3.forEach(word => {col3Height += word.offsetHeight});

// 	alphabetAutoHeight.style.height = Math.max(col1Height,col2Height,col3Height) + 'px'

// } else {
// 	var col1 = wordListOne;
// 	var col2 = wordListTwo;
// 	var col3 = wordListThree;
// 	var col4 = wordListFour;
// 	var col5 = wordListFive;
// 	var col6 = wordListSix;

// 	var col1Height = 0;
// 	col1.forEach(word => {col1Height += word.offsetHeight});

// 	var col2Height = 0;
// 	col2.forEach(word => {col2Height += word.offsetHeight});

// 	var col3Height = 0;
// 	col3.forEach(word => {col3Height += word.offsetHeight});

// 	var col4Height = 0;
// 	col4.forEach(word => {col4Height += word.offsetHeight});

// 	var col5Height = 0;
// 	col5.forEach(word => {col5Height += word.offsetHeight});

// 	var col6Height = 0;
// 	col6.forEach(word => {col6Height += word.offsetHeight});

// 	alphabetAutoHeight.style.height = Math.max(col1Height,col2Height,col3Height,col4Height,col5Height,col6Height) + 'px'

// }



}